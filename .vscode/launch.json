{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/target/debug/challenges",
        "args": ["grade-reviews",
        "-s resources/reviews.csv"],
        "cwd": "${workspaceFolder}"
    }
    ]
}