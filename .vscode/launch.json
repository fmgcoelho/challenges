{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/target/debug/djops",
      "args": [
        "grade",
        "-s",
        "resources/reviews.csv",
        "-f",
        "resources/fbweights.csv"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}
